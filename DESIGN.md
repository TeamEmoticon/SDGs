# 안심글 디자인 시스템

## 1. 분위기와 정체성

안심글은 어르신과 디지털 소외계층이 불안한 문자나 인터넷 글을 천천히 확인할 수 있는 차분한 안전 도우미다. 청록색 안내 표지와 넉넉한 여백, 큰 문장이 핵심 인상이며 위험 표현은 단정 대신 확인을 권하는 말로 전달한다.

## 2. 색상

| 역할 | 토큰 | 값 | 사용처 |
| --- | --- | --- | --- |
| 기본 표면 | `--surface-primary` | `#ffffff` | 카드와 헤더 |
| 보조 표면 | `--surface-secondary` | `#f8fafc` | 페이지 배경과 비활성 영역 |
| 주요 텍스트 | `--text-primary` | `#0f172a` | 제목과 본문 |
| 보조 텍스트 | `--text-secondary` | `#475569` | 도움말과 메타 정보 |
| 기본 테두리 | `--border-default` | `#e2e8f0` | 카드와 입력창 |
| 주요 강조 | `--accent-primary` | `#0d9488` | 주요 버튼, 포커스, 링크 |
| 주의 | `--status-warning` | `#d97706` | 확인이 필요한 안내 |
| 위험 | `--status-danger` | `#c2410c` | 위험 신호 |
| 고위험 | `--status-critical` | `#b91c1c` | 고위험 신호 |

청록색은 행동 가능한 주요 요소에만 사용하며, 위험 단계는 색·큰 제목·설명 문구를 함께 제공한다.

## 3. 타이포그래피

- 글꼴: `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, `Malgun Gothic`, system-ui, sans-serif
- 본문: `1rem`, 1.6 줄간격
- 작은 본문: `0.875rem`, 1.5 줄간격
- 카드 제목: `1.125rem`, 굵게
- 화면 제목: `2.25rem`, 굵게
- 기본 본문은 14px보다 작게 만들지 않는다. 글자 크기 설정은 `normal`, `large`, `xl` 세 단계다.

## 4. 여백과 레이아웃

- 기본 단위: 4px
- 모바일 여백: 16px, 콘텐츠 최대 너비: 1024px
- 카드 안쪽 여백: 20px 또는 24px
- 터치 가능한 버튼의 최소 높이: 44px
- 360px부터 지원하며, 큰 글자에서는 2열 요소를 1열로 전환한다.

## 5. 컴포넌트

### 입력 탭
- 상태: 기본, 선택, hover, focus, disabled
- 모바일에서는 두 라벨이 자연스럽게 줄바꿈될 수 있고, 같은 높이의 탭을 유지한다.

### 분석 결과 카드
- 상태: success, empty, error
- 위험 단계는 제목, 설명, 점수 막대로 함께 설명한다.

### 최근 확인 기록
- 상태: loading, success, empty, error
- 항목은 날짜, 위험 단계, 안전한 요약만 표시하고 다시 보기·삭제를 제공한다.

## 6. 동작

- 상태 전환은 200~500ms의 opacity·transform 애니메이션만 사용한다.
- `prefers-reduced-motion`에서는 필수 아닌 애니메이션을 끈다.
- 모든 버튼은 hover, active, focus-visible, disabled 상태를 제공한다.

## 7. 표면

- 전략: 얇은 테두리와 매우 약한 그림자를 함께 사용한다.
- 카드 그림자는 입력 카드와 모달에만 사용하며, 과도한 유리 효과나 강한 그라데이션은 사용하지 않는다.
