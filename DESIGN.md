# 안심글 디자인 시스템

## 1. 분위기와 정체성

안심글은 어르신과 디지털 소외계층이 불안한 문자나 인터넷 글을 천천히 확인할 수 있는 안전 도우미다. 일반 모드는 따뜻한 밝은 바탕과 청록 행동 버튼을 사용하고, 고대비 모드는 흰 바탕·검은 글자·강한 테두리로 읽기 편하게 전환한다. 위험 표현은 단정 대신 확인을 권하는 말로 전달한다.

## 2. 색상

| 역할 | 토큰 | 값 | 사용처 |
| --- | --- | --- | --- |
| 페이지 바탕 | `--page` | `#fffdf7` | 전체 화면 |
| 주요 표면 | `--surface` | `#ffffff` | 헤더·입력·모달 |
| 보조 표면 | `--surface-muted` | `#f4f1e8` | 탭·안내·접힌 영역 |
| 주요 텍스트 | `--ink` | `#172017` | 제목과 본문 |
| 보조 텍스트 | `--ink-muted` | `#465046` | 도움말과 메타 정보 |
| 기본 테두리 | `--line` | `#b7c0b2` | 구분선·입력창 |
| 주요 행동 | `--action` | `#006b63` | 주요 버튼·링크 |
| 포커스 | `--focus` | `#d46a00` | 키보드 포커스 |
| 주의 | `--warning` | `#9a5800` | 확인이 필요한 안내 |
| 위험 | `--danger` | `#b54708` | 위험 신호 |
| 고위험 | `--critical` | `#a61b1b` | 고위험 신호 |

청록색은 행동 가능한 주요 요소에만 사용하며, 파란색은 대비 판단의 핵심 색으로 사용하지 않는다. 위험 단계는 색·큰 제목·설명 문구·테두리를 함께 제공한다. `data-contrast="high"`에서는 흰 바탕, 검정 텍스트와 테두리, 검정 주요 버튼을 사용하며 색만으로 의미를 전달하지 않는다.

## 3. 타이포그래피

- 글꼴: `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, `Malgun Gothic`, system-ui, sans-serif
- 본문: 기본 `18px`, 1.65 줄간격, 보통 굵기 이상
- 보조 본문: 기본 `16px`, 1.6 줄간격
- 제목: 기본 `22px`, 굵게
- 화면 제목: 기본 `42px`, 굵게, 모바일에서는 `34px`
- 글자 크기 설정은 `normal` 18px, `large` 20px, `xl` 22px 세 단계다. 14px보다 작은 본문은 사용하지 않는다.

## 4. 여백과 레이아웃

- 기본 단위: 4px
- 모바일 여백: 16px, 콘텐츠 최대 너비: 1024px
- 주요 패널 안쪽 여백: 20px 또는 24px
- 일반 버튼 최소 높이: 48px, 주요 행동 버튼 최소 높이: 52px
- 360px부터 지원하며, 큰 글자에서는 2열 요소를 1열로 전환한다.
- 화면에는 한 번에 하나의 주요 작업 패널만 두고, 목록은 카드 격자 대신 구분선으로 나눈다.

## 5. 컴포넌트

### 입력 탭
- 상태: 기본, 선택, hover, focus, disabled
- 모바일에서는 두 라벨이 자연스럽게 줄바꿈될 수 있고, 같은 높이의 탭을 유지한다. 선택 상태는 색뿐 아니라 두꺼운 테두리와 굵은 글씨로 구분한다.

### 분석 결과 카드
- 상태: success, empty, error
- 위험 단계와 지금 할 일은 항상 열어 둔다. 위험 신호·개인정보·분석 근거는 `details`로 접을 수 있으며, 위험·고위험에서는 위험 신호를 기본으로 펼친다.

### 최근 확인 기록
- 상태: loading, success, empty, error
- 항목은 날짜, 위험 단계, 안전한 요약만 표시하고 다시 보기·삭제를 제공한다. 항목은 과도한 카드 대신 구분선 목록으로 구성한다.

### 대비·글자 크기 제어
- 헤더에는 글자 크기와 고대비 제어를 나란히 둔다.
- 고대비 버튼은 `고대비 켜기`와 `일반 화면으로`라는 직접적인 문구, `aria-pressed`, 48px 이상 터치 영역을 제공한다.
- 선택값은 이 기기에 저장하고 새로고침 전에도 적용한다.

## 6. 동작

- 상태 전환은 200~500ms의 opacity·transform 애니메이션만 사용한다.
- `prefers-reduced-motion`에서는 필수 아닌 애니메이션을 끈다.
- 모든 버튼은 hover, active, focus-visible, disabled 상태를 제공한다.

## 7. 표면

- 전략: 주요 표면은 테두리와 여백으로 구분한다.
- 얕은 방향성 그림자는 눌러야 하는 버튼에만 사용한다. 패널·목록에는 과도한 그림자, 유리 효과, 장식용 그라데이션을 사용하지 않는다.
